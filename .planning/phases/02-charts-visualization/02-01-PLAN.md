---
phase: 02-charts-visualization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vite.config.ts
  - src/App.tsx
  - src/utils/chartColors.ts
  - src/utils/aggregation.ts
  - src/utils/aggregation.test.ts
  - src/hooks/useChartData.ts
  - src/components/Charts/MetricChart.tsx
  - src/components/Charts/PeriodSelector.tsx
  - src/components/Charts/ChartHeader.tsx
  - src/components/Charts/CustomTooltip.tsx
  - src/components/Dashboard/MetricTile.tsx
autonomous: true
requirements: [CHRT-01, CHRT-02, CHRT-03]

estimate:
  tokens: 90000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Tapping any metric tile navigates to /chart/:metric (D-01) — Log form no longer opens on tile tap"
    - "Chart screen renders weekly line or bar chart for each metric with real Dexie data (CHRT-01)"
    - "W|M|Y period switcher updates chart data — monthly shows 30-day daily points, yearly shows 12 monthly bars (CHRT-02, CHRT-03, D-03, D-06)"
    - "Chart header shows Avg X.X (min–max) trend-arrow summary stat that re-computes on period switch (D-04)"
    - "FAB on chart screen navigates to /log/:metric (D-02)"
    - "Custom dark-mode tooltip renders on data point tap with value and unit (D-07)"
    - "Each metric chart uses its own accent hex color for stroke/fill (D-08)"
    - "Sleep chart Y-axis shows decimal hours (minutes / 60), not raw minutes (Pitfall 4)"
    - "npm run build exits 0 with no TypeScript errors"
    - "npm test passes — aggregation.ts unit tests all green"
  artifacts:
    - "src/utils/chartColors.ts — exports CHART_HEX: Record<MetricType, string> with all 6 keys"
    - "src/utils/aggregation.ts — exports groupByDay(entries, mode) and aggregateMonthly(daily, mode)"
    - "src/utils/aggregation.test.ts — Vitest tests covering all modes and edge cases"
    - "src/hooks/useChartData.ts — exports useChartData(metric, period)"
    - "src/components/Charts/MetricChart.tsx — /chart/:metric screen"
    - "src/components/Charts/PeriodSelector.tsx — W|M|Y segmented control"
    - "src/components/Charts/ChartHeader.tsx — summary stat bar"
    - "src/components/Charts/CustomTooltip.tsx — dark-mode-aware tooltip"
  key_links:
    - "CHART_HEX in chartColors.ts consumed as Recharts stroke/fill in MetricChart, Sparkline, BmiSection"
    - "aggregation.ts consumed by useChartData.ts — if groupByDay is wrong all chart data is wrong"
    - "/chart/:metric route in App.tsx + MetricTile onClick change (D-01) are the entry point to the whole chart system"
    - "Sleep data must be normalized: duration field (minutes) or computed from beddingTime/wakeTime, then divided by 60"
---

<objective>
Install Recharts v3, create all chart utility infrastructure, and deliver the complete /chart/:metric screen with weekly, monthly, and yearly views for all 6 metrics.

Purpose: Phase 2 success criterion 1 — users can tap any metric and switch between W/M/Y chart views rendered as line or bar charts.
Output: Recharts installed; chartColors.ts, aggregation.ts, useChartData hook; full MetricChart screen with PeriodSelector, ChartHeader, CustomTooltip; MetricTile onClick changed; /chart/:metric route added.
</objective>

<execution_context>
@C:/my-code/vibe-coding/my-health/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/my-health/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-charts-visualization/02-CONTEXT.md
@.planning/phases/02-charts-visualization/02-RESEARCH.md
@.planning/phases/02-charts-visualization/02-PATTERNS.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Install Recharts + wire weight/W chart screen end-to-end</name>
  <files>package.json, src/utils/chartColors.ts, src/components/Charts/MetricChart.tsx, src/App.tsx, src/components/Dashboard/MetricTile.tsx</files>
  <read_first>
    src/App.tsx
    src/components/Dashboard/MetricTile.tsx
    src/utils/constants.ts
    src/store/appStore.ts
    package.json
    .planning/phases/02-charts-visualization/02-RESEARCH.md
  </read_first>
  <action>
    STEP 1 — Install dependencies. From the project root (C:/my-code/vibe-coding/my-health/):
    Run: npm install recharts@^3.10.1 react-is@^19
    This fixes the blank-chart bug on React 19 (RESEARCH.md Pitfall 1). Both packages must be installed together in one command. Verify both appear in package.json dependencies after install.

    STEP 2 — Create src/utils/chartColors.ts. This file exports CHART_HEX: Record from MetricType to hex string. Import MetricType from "@/store/appStore". Define the mapping:
    weight maps to '#3b82f6' (blue-500), sleep maps to '#8b5cf6' (violet-500), steps maps to '#10b981' (emerald-500), water maps to '#06b6d4' (cyan-500), heartRate maps to '#ef4444' (red-500), temperature maps to '#f97316' (orange-500).
    Export CHART_HEX as const. These hex values must match the Tailwind accent colors in constants.ts (RESEARCH.md Pitfall 2 — Tailwind v4 has no JS theme resolver).

    STEP 3 — Create src/components/Charts/ directory and src/components/Charts/MetricChart.tsx as a stub. This stub renders a hardcoded dummy data array (5 points: [{date:'2026-09-08',value:73.5}, ...]) in a Recharts LineChart for the weight metric only. Import ResponsiveContainer, LineChart, Line from 'recharts'. Use ResponsiveContainer width="100%" height={220} — NEVER height="100%" (RESEARCH.md Pitfall 7). Use CHART_HEX['weight'] as the stroke prop on Line. Include CartesianGrid, XAxis, YAxis with the zinc-700 / zinc-400 styling from RESEARCH.md Pattern 4. No real Dexie queries yet. Add a placeholder header with the text "Weight Chart" and a back button (onClick navigates to -1). Wrap in div className="px-4 pt-6" per the screen container pattern.

    STEP 4 — Add the /chart/:metric route to src/App.tsx. Import MetricChart from "./components/Charts/MetricChart". Add Route path="/chart/:metric" element={MetricChart} alongside the existing /log/:metric route, before /settings. No other changes to App.tsx.

    STEP 5 — Modify src/components/Dashboard/MetricTile.tsx per D-01. Change the handleTap function from navigate("/log/" + metric) to navigate("/chart/" + metric). Remove the setSelectedMetric call from handleTap (chart screen uses useParams directly). The useNavigate import already exists. No other changes to MetricTile.tsx in this task.

    SMOKE TEST: After these changes, run npm run build. If the build fails with a TypeScript error about recharts types, add react-is to types or check import paths. If charts render as empty white rectangles (blank chart bug), run: node -e "require('react-is')" to confirm react-is is installed. The build success and non-empty SVG in the running app proves the React 19 + Recharts integration is working.
  </action>
  <acceptance_criteria>
    - package.json dependencies contain "recharts" at ^3.10.1 or higher
    - package.json dependencies contain "react-is" at ^19
    - src/utils/chartColors.ts exports CHART_HEX with keys: weight, sleep, steps, water, heartRate, temperature
    - src/components/Charts/MetricChart.tsx exists and renders a LineChart with ResponsiveContainer height={220}
    - src/App.tsx contains Route path="/chart/:metric"
    - src/components/Dashboard/MetricTile.tsx onClick navigates to /chart/ + metric, not /log/ + metric
    - npm run build exits 0 with no TypeScript errors
  </acceptance_criteria>
  <verify>
    <automated>npm run build 2>&1; echo "EXIT:$?"</automated>
    <fails_when>EXIT:1 in output, or "error TS" appears in build output</fails_when>
  </verify>
  <done>Build passes. Tapping any metric tile in the running app navigates to the stub chart screen showing a LineChart with dummy weight data. The SVG element in the chart is non-empty (proves React 19 + Recharts integration works).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TDD — aggregation utilities (groupByDay + aggregateMonthly)</name>
  <files>package.json, vite.config.ts, src/utils/aggregation.ts, src/utils/aggregation.test.ts</files>
  <read_first>
    src/components/Dashboard/MetricTile.tsx
    .planning/phases/02-charts-visualization/02-RESEARCH.md
    .planning/phases/02-charts-visualization/02-PATTERNS.md
    vite.config.ts
  </read_first>
  <behavior>
    - groupByDay([], 'average') returns []
    - groupByDay([{date:'2026-09-10',value:73}], 'average') returns [{date:'2026-09-10',value:73}]
    - groupByDay([{date:'2026-09-10',value:72},{date:'2026-09-10',value:74}], 'average') returns [{date:'2026-09-10',value:73}]
    - groupByDay([{date:'2026-09-10',value:100},{date:'2026-09-10',value:200}], 'sum') returns [{date:'2026-09-10',value:300}]
    - groupByDay results are sorted ascending by date string
    - aggregateMonthly([{date:'2026-09-01',value:10},{date:'2026-09-15',value:20}], 'average') returns [{date:'2026-09',value:15}]
    - aggregateMonthly([{date:'2026-09-01',value:10},{date:'2026-09-15',value:20}], 'sum') returns [{date:'2026-09',value:30}]
    - aggregateMonthly results are sorted ascending by month string
    - aggregateMonthly handles empty input by returning []
  </behavior>
  <action>
    STEP 1 — Install Vitest. From project root: npm install -D vitest
    Add "test": "vitest run" to the scripts section of package.json.
    Add "/// reference types vitest/globals ///" triple-slash directive at the top of vite.config.ts (write as a TypeScript triple-slash comment with forward slashes, types="vitest/globals"), then add a test block inside the defineConfig object: test: { globals: true, environment: 'node' }. The vite.config.ts still imports defineConfig from 'vite' (not vitest/config) — the triple-slash reference adds the type overlay for the test property.

    STEP 2 — Write tests FIRST (RED phase). Create src/utils/aggregation.test.ts. Import describe, it, expect from 'vitest'. Define the DailyPoint interface inline in the test file. Write tests for groupByDay and aggregateMonthly covering all cases listed in the behavior block above. Run npm test — it should FAIL because aggregation.ts does not exist yet.

    STEP 3 — Implement aggregation.ts (GREEN phase). Create src/utils/aggregation.ts. Export interface DailyPoint as { date: string; value: number }. Export function groupByDay(entries: { date: string; value: number }[], mode: 'average' | 'sum'): DailyPoint[]. Implementation: build a Map from date to number[] by iterating entries; then convert to sorted array; for 'average' mode divide sum by count, for 'sum' mode use raw sum. Export function aggregateMonthly(daily: DailyPoint[], mode: 'average' | 'sum'): DailyPoint[]. Implementation: bucket each DailyPoint by format(new Date(d.date + 'T00:00:00'), 'yyyy-MM') from date-fns; sort and reduce per mode. Import format from 'date-fns'. Run npm test — all tests must PASS.

    STEP 4 — Refactor (REFACTOR phase). Review both functions for clarity and edge cases (empty arrays, single entries, undefined values). Ensure the month bucketing uses 'T00:00:00' suffix to prevent timezone shift (RESEARCH.md Pitfall 3). Run npm test again after any refactor — must still PASS.
  </action>
  <acceptance_criteria>
    - src/utils/aggregation.test.ts exists and contains at least 9 test cases
    - src/utils/aggregation.ts exports groupByDay and aggregateMonthly with correct TypeScript signatures
    - npm test exits 0 — all aggregation tests pass
    - vite.config.ts contains "test:" block with globals and environment
    - package.json scripts contains "test": "vitest run"
  </acceptance_criteria>
  <verify>
    <automated>npm test 2>&1; echo "EXIT:$?"</automated>
    <fails_when>EXIT:1 in output, or any "FAIL" or "AssertionError" appears in test output</fails_when>
  </verify>
  <done>npm test exits 0. All aggregation unit tests pass (GREEN). No TypeScript errors in aggregation.ts. The functions correctly handle empty arrays, single entries, same-day averaging, same-day summing, and monthly bucketing.</done>
</task>

<task type="auto">
  <name>Task 3: Complete MetricChart — real data, all metrics, all periods, all features</name>
  <files>src/hooks/useChartData.ts, src/components/Charts/MetricChart.tsx, src/components/Charts/PeriodSelector.tsx, src/components/Charts/ChartHeader.tsx, src/components/Charts/CustomTooltip.tsx</files>
  <read_first>
    src/utils/aggregation.ts
    src/utils/chartColors.ts
    src/utils/constants.ts
    src/store/appStore.ts
    src/db/schema.ts
    src/components/Charts/MetricChart.tsx
    src/components/Log/LogScreen.tsx
    src/components/ui/button.tsx
    .planning/phases/02-charts-visualization/02-RESEARCH.md
    .planning/phases/02-charts-visualization/02-PATTERNS.md
  </read_first>
  <action>
    STEP 1 — Create src/hooks/ directory and src/hooks/useChartData.ts. This hook accepts metric: MetricType and period: 'W' | 'M' | 'Y' and returns { data: DailyPoint[], prevData: DailyPoint[], isLoading: boolean }. prevData is the equivalent previous period (for trend arrow in D-04: previous 7/30/365 days). Use the useEffect + async cancellation pattern from MetricTile.tsx (include a cancelled flag, set to true in cleanup). Inside the async function:

    1. Compute today as format(new Date(), 'yyyy-MM-dd') from date-fns.
    2. Compute daysBack: W=7, M=30, Y=365.
    3. Compute start and prevStart using subDays from date-fns: start = format(subDays(new Date(), daysBack), 'yyyy-MM-dd'); prevStart = format(subDays(new Date(), daysBack * 2), 'yyyy-MM-dd').
    4. Query the correct Dexie table using db imported from "@/db/schema". Use .where('date').between(start, today, true, true).toArray() for current period and .where('date').between(prevStart, start, true, false).toArray() for previous period.
    5. Normalize raw entries to { date: string; value: number } per metric: weight uses e.value; sleep uses (e.duration ?? differenceInMinutes(parseISO(e.wakeTime), parseISO(e.beddingTime))) / 60 (imports differenceInMinutes, parseISO from date-fns); steps uses e.steps; water uses e.ml; heartRate uses e.bpm; temperature uses e.celsius.
    6. Call groupByDay(rawPoints, mode) where mode is 'sum' for steps and water, 'average' for all others.
    7. For Y period, call aggregateMonthly(daily, mode) on top of groupByDay result.
    8. If not cancelled, call setData(result) and setPrevData(prevResult).
    Import DailyPoint from "@/utils/aggregation". Table references: db.weights, db.sleepEntries, db.stepEntries, db.waterEntries, db.heartRates, db.temperatures.

    STEP 2 — Create src/components/Charts/PeriodSelector.tsx. Props: selected: 'W' | 'M' | 'Y'; onSelect: (p: 'W' | 'M' | 'Y') => void. Render a flex row with three Button components from "@/components/ui/button". The active period uses variant="default", inactive periods use variant="outline". Label the buttons "W", "M", "Y". Apply size="sm" to each button. Wrap in div className="flex gap-1".

    STEP 3 — Create src/components/Charts/ChartHeader.tsx. Props: metric: MetricType; data: DailyPoint[]; prevData: DailyPoint[]; period: 'W' | 'M' | 'Y'. This component computes avg, min, max from data using Math.min, Math.max, and Array.reduce. Compute prevAvg from prevData the same way. Render the trend arrow using lucide-react: ArrowUp className "size-4 text-emerald-500" when delta > 0.01, ArrowDown className "size-4 text-red-500" when delta < -0.01, Minus className "size-4 text-zinc-400" otherwise. Delta = (avg - prevAvg) / prevAvg; guard against prevAvg === 0 by returning Minus. Render the metric name (from METRIC_CONFIG[metric].label) with accent color from METRIC_CONFIG[metric].accentColor. Render the summary stat as "Avg X.X (min–max)" with trend icon inline. Use text-sm text-zinc-400 for secondary text, text-white for values. Import METRIC_CONFIG from "@/utils/constants".

    STEP 4 — Create src/components/Charts/CustomTooltip.tsx. Props interface CustomTooltipProps extends TooltipProps<number, string> with unit: string added. Import TooltipProps from 'recharts'. If not active or payload is empty return null. Render a div with className "rounded-lg border border-zinc-700 bg-zinc-900 px-3 py-2 shadow-lg text-sm". Show label in p className "text-zinc-400 text-xs mb-1". Show value + unit in p className "text-white font-semibold". For value display call payload[0].value?.toFixed(1). Do not use dangerouslySetInnerHTML (security: XSS via tooltip, T-02-02 mitigated by JSX escaping). Import type from 'recharts' only.

    STEP 5 — Rewrite src/components/Charts/MetricChart.tsx to use real data. Structure:
    - useParams to get metric as string, validate it is a valid MetricType (check against ['weight','sleep','steps','water','heartRate','temperature']); if invalid, navigate(-1) and return null.
    - useState<'W'|'M'|'Y'>('W') for period.
    - Call useChartData(metric as MetricType, period) to get data, prevData, isLoading.
    - Auto-select chart type per D-05: METRIC_CHART_TYPE constant maps weight/sleep/heartRate/temperature to 'line', steps/water to 'bar'. For Y period always use 'bar' (D-06).
    - Render structure: div px-4 pt-6 with flex header row (back button on left using navigate(-1) with ChevronLeft from lucide-react, metric name in center, nothing on right for now). ChartHeader below header. PeriodSelector below that. ResponsiveContainer width="100%" height={220} containing either LineChart or BarChart based on chartType. XAxis tickFormatter: for Y period format(new Date(date + '-01T00:00:00'), 'MMM') from date-fns, else format(new Date(date + 'T00:00:00'), 'MMM d'). XAxis tick fill '#71717a' fontSize 11, axisLine false, tickLine false. YAxis same styling. CartesianGrid strokeDasharray="3 3" stroke="#27272a". Tooltip content={CustomTooltip instance with unit={METRIC_CONFIG[metric as MetricType].unit}}. Line/Bar uses CHART_HEX[metric as MetricType] as stroke/fill. Line: type="monotone" strokeWidth={2} dot={false} connectNulls={false}. Bar: radius={[2,2,0,0]}. FAB (D-02): fixed bottom-6 right-6 z-10 button with Plus icon from lucide-react, rounded-full bg-white text-black p-4 shadow-lg, onClick navigates to "/log/" + metric.
    - isLoading state renders a div with text "Loading..." centered.
  </action>
  <acceptance_criteria>
    - src/hooks/useChartData.ts exports useChartData(metric, period) returning {data, prevData, isLoading}
    - src/components/Charts/PeriodSelector.tsx renders 3 Button elements with W, M, Y labels
    - src/components/Charts/ChartHeader.tsx renders avg/min/max stat and a trend icon
    - src/components/Charts/CustomTooltip.tsx does not use dangerouslySetInnerHTML
    - src/components/Charts/MetricChart.tsx uses useChartData and renders LineChart or BarChart based on metric type
    - Yearly period (Y) always renders BarChart regardless of metric type
    - FAB exists in MetricChart.tsx and navigates to /log/:metric
    - npm run build exits 0 with no TypeScript errors
    - npm run typecheck exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run build 2>&1; echo "EXIT:$?"</automated>
    <fails_when>EXIT:1 in output, or "error TS" in build output</fails_when>
  </verify>
  <done>npm run build exits 0. All 6 metrics navigate to their chart screens. W/M/Y switcher updates the chart. Yearly view shows bar chart. Sleep chart Y-axis shows decimal hours. Trend arrow updates on period switch. FAB opens log form for that metric. Custom tooltip appears on data point interaction.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage → BMI calc | Height value from localStorage is user-entered string; must be validated (covered in Plan 02-04) |
| Recharts SVG → DOM | Recharts renders SVG from data; tooltip HTML rendered by CustomTooltip — no user HTML injected |
| URL param → metric type | useParams metric string must be validated against MetricType union before use |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | useParams metric validation in MetricChart.tsx | medium | mitigate | Validate metric against allowlist ['weight','sleep','steps','water','heartRate','temperature']; navigate(-1) if invalid — prevents unknown route crashes |
| T-02-02 | Tampering | CustomTooltip.tsx — XSS via tooltip content | low | accept | React JSX escaping prevents XSS by default; no dangerouslySetInnerHTML used; value comes from numeric Dexie data |
| T-02-03 | Tampering | npm install recharts + react-is | high | mitigate | Package legitimacy audit in RESEARCH.md: recharts OK (approved), react-is SUS (approved — Meta official package with 258M+/wk downloads); verify npm registry checksums via standard npm install |
</threat_model>

<verification>
After all tasks complete:
1. npm run build exits 0 — no TypeScript errors
2. npm test exits 0 — aggregation.ts unit tests pass
3. grep -c "CHART_HEX" src/utils/chartColors.ts returns 1
4. grep -c "groupByDay" src/utils/aggregation.ts returns 1
5. grep -c "aggregateMonthly" src/utils/aggregation.ts returns 1
6. grep -c "/chart/:metric" src/App.tsx returns 1
7. grep -c "navigate.*chart" src/components/Dashboard/MetricTile.tsx returns 1 (not /log/:metric)
8. src/components/Charts/ directory contains MetricChart.tsx, PeriodSelector.tsx, ChartHeader.tsx, CustomTooltip.tsx
9. src/hooks/ directory contains useChartData.ts
</verification>

<success_criteria>
All 6 metric tiles navigate to /chart/:metric. Chart screen shows W/M/Y period switcher. W and M show daily data points as line or bar. Y shows 12 monthly aggregate bars. Header shows "Avg X.X (min–max) arrow" that updates on period switch. FAB opens log form. Custom dark-mode tooltip appears on chart interaction. Build passes with no TypeScript errors. Aggregation unit tests pass.
</success_criteria>

<output>
Create `.planning/phases/02-charts-visualization/02-01-SUMMARY.md` when done.

## Artifacts this phase produces

| Symbol | File | Consumed By |
|--------|------|-------------|
| CHART_HEX | src/utils/chartColors.ts | MetricChart, Sparkline (02-03), BmiSection (02-04) |
| groupByDay | src/utils/aggregation.ts | useChartData |
| aggregateMonthly | src/utils/aggregation.ts | useChartData |
| useChartData | src/hooks/useChartData.ts | MetricChart, Sparkline (02-03) |
| MetricChart | src/components/Charts/MetricChart.tsx | App.tsx route, BmiSection (02-04) |
| PeriodSelector | src/components/Charts/PeriodSelector.tsx | MetricChart |
| ChartHeader | src/components/Charts/ChartHeader.tsx | MetricChart |
| CustomTooltip | src/components/Charts/CustomTooltip.tsx | MetricChart, BmiSection (02-04) |
| /chart/:metric route | src/App.tsx | MetricTile onClick (D-01) |
</output>
