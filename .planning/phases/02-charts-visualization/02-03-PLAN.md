---
phase: 02-charts-visualization
plan: 03
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/components/Charts/Sparkline.tsx
  - src/components/Dashboard/MetricTile.tsx
autonomous: true
requirements: [DASH-02]

estimate:
  tokens: 45000
  raw_tokens: 22000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Each metric tile on the Dashboard shows a 14-day mini sparkline (line chart, no axes) at the bottom of the tile (D-14)"
    - "Sparkline uses the metric's accent hex color (same color as tile accent — D-08)"
    - "When no data exists for a metric in the last 14 days, the tile shows 'Start logging to see trends' in place of the sparkline (D-15)"
    - "Target progress bars and red/yellow/green color coding are NOT in this plan — deferred to Phase 3 per D-16"
    - "npm run build exits 0 with no TypeScript errors"
  artifacts:
    - "src/components/Charts/Sparkline.tsx — exports Sparkline component accepting metric and accentHex props"
    - "src/components/Dashboard/MetricTile.tsx — renders Sparkline below value display"
  key_links:
    - "Sparkline reuses useChartData(metric, 'W') from 02-01 — no separate data-fetching logic needed"
    - "CHART_HEX from chartColors.ts (02-01) provides the hex stroke color for the sparkline Line"
    - "MetricTile.tsx already has its onClick changed in 02-01 — this plan only adds the Sparkline child"
---

<objective>
Add 14-day mini sparklines to each metric tile on the Dashboard, delivering trend visibility at a glance.

Purpose: Phase 2 delivers sparklines (D-14, D-15) as the tile visual enhancement for this phase. DASH-02 target progress bars and red/yellow/green color coding require Phase 3 targets and are deferred to Phase 3 per D-16.
Output: Sparkline.tsx component; MetricTile.tsx updated to embed sparkline.

Note on DASH-02 scope: This plan addresses DASH-02 by delivering 14-day mini sparklines (D-14) on metric tiles as the Phase 2 tile enhancement. The remaining DASH-02 sub-features (target progress bar, red/yellow/green color indicator) depend on Phase 3 targets and are explicitly deferred per D-16 in the locked decisions.
</objective>

<execution_context>
@C:/my-code/vibe-coding/my-health/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/my-health/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-charts-visualization/02-CONTEXT.md
@.planning/phases/02-charts-visualization/02-RESEARCH.md
@.planning/phases/02-charts-visualization/02-PATTERNS.md
@.planning/phases/02-charts-visualization/02-01-SUMMARY.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Sparkline component + MetricTile integration (D-14, D-15)</name>
  <files>src/components/Charts/Sparkline.tsx, src/components/Dashboard/MetricTile.tsx</files>
  <read_first>
    src/components/Dashboard/MetricTile.tsx
    src/utils/chartColors.ts
    src/hooks/useChartData.ts
    .planning/phases/02-charts-visualization/02-RESEARCH.md
    .planning/phases/02-charts-visualization/02-PATTERNS.md
  </read_first>
  <action>
    STEP 1 — Create src/components/Charts/Sparkline.tsx.

    Props interface: SparklineProps with metric: MetricType (imported from "@/store/appStore") and accentHex: string.

    Data fetching: Call useChartData(metric, 'W') from "@/hooks/useChartData". This returns 14 days of aggregated daily data (the W period is 7 days per the hook — adjust: for sparkline we want 14 days, which is the M period returning 30 days. Use 'W' period for 7-day data OR expose a new period for 14 days. Because useChartData uses W=7 days, use period='M' and take the last 14 entries with .slice(-14) on the returned data array). Destructure { data, isLoading } from the hook call.

    Empty state (D-15): If not isLoading and data.length === 0, render: div className="h-[40px] flex items-center" containing span className="text-xs text-gray-400" with text content "Start logging to see trends".

    Chart: If data has entries, render ResponsiveContainer from 'recharts' with width="100%" height={40}. Inside, render LineChart with data={data} and no margin. Inside LineChart, render only Line component: type="monotone", dataKey="value", stroke={accentHex}, strokeWidth={1.5}, dot={false}. Do NOT include XAxis, YAxis, CartesianGrid, or Tooltip — sparkline is purely visual (RESEARCH.md Pattern 8). No isAnimationActive prop needed.

    isLoading state: render div className="h-[40px]" (empty placeholder, same height as sparkline).

    STEP 2 — Modify src/components/Dashboard/MetricTile.tsx.

    Import CHART_HEX from "@/utils/chartColors" at the top of the file.
    Import Sparkline from "@/components/Charts/Sparkline".

    Inside the component's returned JSX, add Sparkline as the last element before the closing tag of the button element (or the outermost tile div). Pass metric={metric} and accentHex={CHART_HEX[metric]}.

    The tile already shows the value display and variant labels. The Sparkline goes BELOW all value content, at the bottom of the tile card. Add a div className="mt-2" wrapping Sparkline to create visual separation from the value display above.

    Do NOT add target progress bars, percentage displays, or color indicator borders (D-16 deferral — those are Phase 3 features). Do NOT change the tile's onClick handler — that was changed in Plan 02-01 to navigate to /chart/:metric.
  </action>
  <acceptance_criteria>
    - src/components/Charts/Sparkline.tsx exists and imports useChartData and ResponsiveContainer, LineChart, Line from 'recharts'
    - Sparkline renders height={40} ResponsiveContainer with only a Line component (no XAxis, YAxis, CartesianGrid, Tooltip)
    - Sparkline shows "Start logging to see trends" span text when data is empty
    - src/components/Dashboard/MetricTile.tsx imports Sparkline and CHART_HEX
    - MetricTile.tsx renders Sparkline with metric and accentHex={CHART_HEX[metric]} props
    - npm run build exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run build 2>&1; echo "EXIT:$?"</automated>
    <fails_when>EXIT:1 in output, or "error TS" in build output</fails_when>
  </verify>
  <done>Build passes. Each metric tile on the Dashboard shows a compact 14-day line sparkline at the bottom using the metric's accent color. Tiles with no recent data show the "Start logging to see trends" message instead of an empty chart area.</done>
</task>

<task type="auto">
  <name>Task 2: Verify CHART_HEX coverage and empty state — build and grep checks</name>
  <files>src/components/Charts/Sparkline.tsx</files>
  <read_first>
    src/utils/chartColors.ts
    src/components/Charts/Sparkline.tsx
    src/components/Dashboard/MetricTile.tsx
  </read_first>
  <action>
    STEP 1 — Verify CHART_HEX covers all MetricType keys. Run: grep -c "temperature" src/utils/chartColors.ts. If the result is 0, chartColors.ts is missing the temperature key. Add it: temperature: '#f97316'. Verify all 6 keys are present: weight, sleep, steps, water, heartRate, temperature.

    STEP 2 — Verify Sparkline empty state text. Confirm the exact text "Start logging to see trends" appears in Sparkline.tsx. This text must be exactly as specified in D-15. Run: grep -c "Start logging to see trends" src/components/Charts/Sparkline.tsx. If result is 0, fix the empty state message.

    STEP 3 — Verify ResponsiveContainer height is a fixed pixel value, not "100%". Run: grep -c 'height={40}' src/components/Charts/Sparkline.tsx. Result must be at least 1. If result is 0, find and fix the height prop (RESEARCH.md Pitfall 7 — height="100%" resolves to 0 in auto-height parents).

    STEP 4 — Verify MetricTile onClick was NOT accidentally reverted. Run: grep -c 'navigate.*chart' src/components/Dashboard/MetricTile.tsx. Result must be at least 1. If 0, the onClick was lost — re-apply the D-01 change from Plan 02-01.

    STEP 5 — Final build. Run npm run build. Confirm exit 0 with no TypeScript errors.

    STEP 6 — Verify MetricTile renders both a value display and a Sparkline. Run: grep -c "Sparkline" src/components/Dashboard/MetricTile.tsx. Result must be at least 2 (import + usage). If result is less than 2, Sparkline was imported but not rendered — add it to the JSX return.
  </action>
  <acceptance_criteria>
    - grep -c "temperature" src/utils/chartColors.ts returns at least 1
    - grep -c "height={40}" src/components/Charts/Sparkline.tsx returns at least 1
    - grep -c "navigate.*chart" src/components/Dashboard/MetricTile.tsx returns at least 1
    - grep -c "Sparkline" src/components/Dashboard/MetricTile.tsx returns at least 2 (import + JSX)
    - npm run build exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run build 2>&1; echo "EXIT:$?"</automated>
    <fails_when>EXIT:1 in output, or "error TS" in build output</fails_when>
  </verify>
  <done>All grep checks pass. Build exits 0. CHART_HEX has all 6 metric keys. Sparkline has fixed pixel height. MetricTile onClick still navigates to /chart/:metric. Sparkline is both imported and rendered in MetricTile.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dexie → Sparkline data | Read-only query via useChartData; same pattern as MetricChart |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-06 | Tampering | Sparkline.tsx — ResponsiveContainer height="100%" | low | mitigate | Always use height={40} (fixed pixel) per RESEARCH.md Pitfall 7; height="100%" resolves to 0 in auto-height flex parents and produces invisible chart |
| T-02-07 | Information Disclosure | CHART_HEX hardcoded hex values | low | accept | Hex values are public design tokens matching Tailwind palette; no sensitive data exposed |
</threat_model>

<verification>
After all tasks complete:
1. npm run build exits 0
2. grep -c "Sparkline" src/components/Dashboard/MetricTile.tsx returns 2 or more
3. grep -c "height={40}" src/components/Charts/Sparkline.tsx returns 1
4. grep -v "^//" src/components/Charts/Sparkline.tsx | grep -c "XAxis\|YAxis\|CartesianGrid\|Tooltip" returns 0 (no axes in sparkline)
5. grep -c "navigate.*chart" src/components/Dashboard/MetricTile.tsx returns 1
</verification>

<success_criteria>
All 6 metric tiles show a 14-day mini sparkline at the bottom using the tile's accent color. Tiles with no data in the last 14 days show "Start logging to see trends" message. Sparkline is purely visual — no axes, no tooltip, no grid. Build passes with no TypeScript errors.
</success_criteria>

<output>
Create `.planning/phases/02-charts-visualization/02-03-SUMMARY.md` when done.

## Artifacts this phase produces

| Symbol | File | Consumed By |
|--------|------|-------------|
| Sparkline | src/components/Charts/Sparkline.tsx | MetricTile.tsx |
</output>
