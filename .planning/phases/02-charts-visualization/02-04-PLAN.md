---
phase: 02-charts-visualization
plan: 04
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/utils/bmi.ts
  - src/utils/bmi.test.ts
  - src/hooks/useBmiData.ts
  - src/components/Charts/BmiSection.tsx
  - src/components/Charts/MetricChart.tsx
autonomous: true
requirements: [BMI-01, BMI-02]

estimate:
  tokens: 65000
  raw_tokens: 32000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Height stored in localStorage under key 'myhealth-height' (already implemented in Phase 1 UX-04)"
    - "BMI is auto-calculated from weight entries using height from localStorage (BMI-01)"
    - "BMI appears as a separate section BELOW the weight chart — not as a second Y-axis line (D-09, BMI-02)"
    - "BMI section shows current BMI value, category label, and a mini trend LineChart"
    - "When height is not set or is <= 0, BMI section shows 'Set height in Settings to see BMI' (RESEARCH.md Pitfall 5)"
    - "BMI categories: Underweight < 18.5, Normal 18.5-24.9, Overweight 25-29.9, Obese >= 30 (verbatim from CONTEXT.md Specific Ideas)"
    - "npm run build exits 0 and npm test exits 0"
  artifacts:
    - "src/utils/bmi.ts — exports calcBmi(weightKg, heightCm) and bmiCategory(bmi)"
    - "src/utils/bmi.test.ts — Vitest tests covering all BMI categories and edge cases"
    - "src/hooks/useBmiData.ts — exports useBmiData() returning {bmiData, currentBmi, category, error}"
    - "src/components/Charts/BmiSection.tsx — BMI mini-section component"
    - "src/components/Charts/MetricChart.tsx — BmiSection rendered below weight chart only"
  key_links:
    - "bmi.ts is pure (no deps) — consumed by useBmiData.ts which adds Dexie querying and localStorage reading"
    - "BmiSection only renders inside MetricChart when metric === 'weight' (D-09)"
    - "Height guard in useBmiData: if (!heightCm || heightCm <= 0) return error:'no-height' — prevents divide-by-zero (T-02-08)"
    - "useBmiData queries db.weights for ALL time (no date range) to build full BMI history chart"
---

<objective>
Add auto-calculated BMI to the weight chart screen as a separate section below the chart, backed by height from Settings and all weight history.

Purpose: Phase 2 success criterion 4 — user enters height once in settings; BMI is auto-calculated from all weight entries and shown below the weight chart.
Output: bmi.ts utilities (TDD), useBmiData hook, BmiSection component, MetricChart.tsx updated to show BmiSection for weight only.
</objective>

<execution_context>
@C:/my-code/vibe-coding/my-health/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/my-health/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-charts-visualization/02-CONTEXT.md
@.planning/phases/02-charts-visualization/02-RESEARCH.md
@.planning/phases/02-charts-visualization/02-PATTERNS.md
@.planning/phases/02-charts-visualization/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD — bmi.ts utilities + useBmiData hook</name>
  <files>src/utils/bmi.ts, src/utils/bmi.test.ts, src/hooks/useBmiData.ts</files>
  <read_first>
    src/utils/aggregation.ts
    src/db/schema.ts
    src/components/Settings/Settings.tsx
    vite.config.ts
    .planning/phases/02-charts-visualization/02-CONTEXT.md
    .planning/phases/02-charts-visualization/02-RESEARCH.md
  </read_first>
  <behavior>
    - calcBmi(70, 175) returns approximately 22.86 (within 0.01 of 70 / (1.75 * 1.75))
    - calcBmi(50, 160) returns approximately 19.53
    - calcBmi(90, 180) returns 27.78 (Overweight)
    - bmiCategory(18.4) returns 'Underweight'
    - bmiCategory(18.5) returns 'Normal'
    - bmiCategory(24.9) returns 'Normal'
    - bmiCategory(25.0) returns 'Overweight'
    - bmiCategory(29.9) returns 'Overweight'
    - bmiCategory(30.0) returns 'Obese'
    - bmiCategory(35.0) returns 'Obese'
  </behavior>
  <action>
    STEP 1 — RED phase. Create src/utils/bmi.test.ts. Import { describe, it, expect } from 'vitest'. Write describe blocks for calcBmi and bmiCategory. Write test cases matching every behavior listed in the behavior block above. Use expect(calcBmi(70, 175)).toBeCloseTo(22.86, 1) for floating-point comparisons. Use expect(bmiCategory(18.4)).toBe('Underweight') for category tests. Import calcBmi and bmiCategory from './bmi'. Run npm test — it must FAIL because bmi.ts does not exist yet.

    STEP 2 — GREEN phase. Create src/utils/bmi.ts. Export function calcBmi(weightKg: number, heightCm: number): number. Implementation: const heightM = heightCm / 100; return weightKg / (heightM * heightM). Do not guard against zero here — the guard is in useBmiData (security separation of concerns: bmi.ts is a pure math function; useBmiData validates input).

    Export function bmiCategory(bmi: number): 'Underweight' | 'Normal' | 'Overweight' | 'Obese'. Implementation using exact thresholds from CONTEXT.md Specific Ideas: if (bmi < 18.5) return 'Underweight'; if (bmi < 25) return 'Normal'; if (bmi < 30) return 'Overweight'; return 'Obese'.

    No imports needed in bmi.ts — it is a pure math utility with no dependencies. Run npm test — all tests must PASS.

    STEP 3 — REFACTOR phase. Review bmi.ts for clarity. Confirm the return type union is correct and the thresholds exactly match the CONTEXT.md values (18.5, 25, 30). Run npm test — still PASS.

    STEP 4 — Create src/hooks/useBmiData.ts. This hook reads height from localStorage and queries all weight entries to build a BMI data series.

    Export interface BmiPoint as { date: string; bmi: number }.
    Export interface BmiResult as { bmiData: BmiPoint[]; currentBmi: number | null; category: 'Underweight' | 'Normal' | 'Overweight' | 'Obese' | null; error: 'no-height' | null }.
    Export function useBmiData(): BmiResult.

    Implementation: Use useState for bmiData, currentBmi, category, error. Use useEffect + async with cancellation flag.

    Inside async load():
    1. Read height: const heightCm = parseFloat(localStorage.getItem('myhealth-height') ?? ''). Guard: if (!heightCm || heightCm <= 0 || isNaN(heightCm)) { if (!cancelled) setError('no-height'); return; }. This prevents divide-by-zero (T-02-08, RESEARCH.md Pitfall 5).
    2. Query ALL weights (no date range): const allWeights = await db.weights.orderBy('date').toArray().
    3. Group weights by day (average per day): use groupByDay from "@/utils/aggregation" with mode 'average'.
    4. Map each DailyPoint to BmiPoint: { date: d.date, bmi: calcBmi(d.value, heightCm) } using calcBmi from "@/utils/bmi".
    5. Get current BMI: last entry in bmiData (most recent date). If bmiData.length > 0: setCurrentBmi(bmiData[bmiData.length - 1].bmi); setCategory(bmiCategory(currentBmiValue)).
    6. If not cancelled: setBmiData(result).

    Import { db } from "@/db/schema", { groupByDay } from "@/utils/aggregation", { calcBmi, bmiCategory } from "@/utils/bmi", { useState, useEffect } from 'react'.
  </action>
  <acceptance_criteria>
    - src/utils/bmi.test.ts exists with at least 10 test cases
    - src/utils/bmi.ts exports calcBmi and bmiCategory with correct TypeScript return types
    - npm test exits 0 — all bmi tests pass alongside aggregation tests
    - bmiCategory thresholds are exactly: Underweight < 18.5, Normal 18.5-24.9, Overweight 25-29.9, Obese >= 30
    - src/hooks/useBmiData.ts guards heightCm <= 0 before dividing and returns error: 'no-height'
    - useBmiData imports from "@/utils/bmi" and "@/utils/aggregation" (no inline BMI math)
  </acceptance_criteria>
  <verify>
    <automated>npm test 2>&1; echo "EXIT:$?"</automated>
    <fails_when>EXIT:1 in output, or any "FAIL" or "AssertionError" in test output</fails_when>
  </verify>
  <done>npm test exits 0. All BMI unit tests pass: calcBmi returns correct floating-point values, bmiCategory returns correct string for all boundary values. useBmiData hook exists with height guard and full BMI data series computation.</done>
</task>

<task type="tracer">
  <name>Task 2: BmiSection component + wire into MetricChart below weight chart (D-09)</name>
  <files>src/components/Charts/BmiSection.tsx, src/components/Charts/MetricChart.tsx</files>
  <read_first>
    src/hooks/useBmiData.ts
    src/utils/chartColors.ts
    src/components/Charts/MetricChart.tsx
    src/components/Dashboard/MetricTile.tsx
    .planning/phases/02-charts-visualization/02-CONTEXT.md
    .planning/phases/02-charts-visualization/02-PATTERNS.md
  </read_first>
  <action>
    STEP 1 — Create src/components/Charts/BmiSection.tsx.

    Call useBmiData() from "@/hooks/useBmiData" to get bmiData, currentBmi, category, error.

    No-height state: If error === 'no-height', render a div className="mt-6 px-0 py-4 rounded-xl bg-zinc-900 text-center" containing span className="text-sm text-gray-400" with text "Set height in Settings to see BMI". No chart rendered. No crash.

    No-data state: If bmiData.length === 0 and error is null, render the same container with text "Log weight to calculate BMI".

    Normal state: Render a titled section below the weight chart.
    - Section heading: span className="text-xs uppercase tracking-widest mb-2 text-blue-500" with text "BMI". (Uses weight accent color blue-500 since BMI derives from weight.)
    - Current BMI display: div className="flex items-baseline gap-2 mb-1" containing span className="text-3xl font-semibold text-white leading-none" with {currentBmi?.toFixed(1)} and span className="text-sm text-gray-400" with {category}.
    - BMI category color hint: Add a small colored dot or badge next to category. Map categories to colors: Underweight='text-blue-400', Normal='text-emerald-400', Overweight='text-yellow-400', Obese='text-red-400'. Apply these as className on the category span.
    - BMI mini chart: Render ResponsiveContainer width="100%" height={120} containing LineChart with data={bmiData} (dataKey="bmi", not "value"). XAxis with tickFormatter showing only month label (format last point's date to 'MMM'). YAxis with tick fill '#71717a' fontSize 11. Line type="monotone" dataKey="bmi" stroke="#3b82f6" (weight blue) strokeWidth={2} dot={false}. Include CustomTooltip from "@/components/Charts/CustomTooltip" with unit="BMI".
    - Reference lines: Add a ReferenceLine at y={18.5} stroke="#71717a" strokeDasharray="3 3" label={{ value: '18.5', fill: '#71717a', fontSize: 10 }} and at y={25} and y={30} with same styling. Import ReferenceLine from 'recharts'. These show the BMI category thresholds on the mini chart.

    Wrap the entire BmiSection in div className="mt-8 p-4 rounded-xl bg-zinc-900".

    STEP 2 — Modify src/components/Charts/MetricChart.tsx to include BmiSection.

    Import BmiSection from "@/components/Charts/BmiSection".

    In the JSX return of MetricChart, after the ResponsiveContainer chart div, add: {metric === 'weight' && <BmiSection />}. The BmiSection renders only when viewing the weight chart (D-09). No other metric shows BMI.

    Ensure this conditional is inside the main scroll container (not fixed-positioned) so it appears below the chart when scrolling.

    STEP 3 — Run npm run build and npm test. Both must pass. Check for TypeScript errors around the ReferenceLine import (it should be available in recharts@^3).
  </action>
  <acceptance_criteria>
    - src/components/Charts/BmiSection.tsx exists and calls useBmiData()
    - BmiSection renders "Set height in Settings to see BMI" when error === 'no-height'
    - BmiSection renders a LineChart with dataKey="bmi" and height={120} when data exists
    - BmiSection includes ReferenceLine components at y=18.5, y=25, y=30
    - src/components/Charts/MetricChart.tsx conditionally renders BmiSection only when metric === 'weight'
    - No dual Y-axis — BmiSection is a separate section, not a second Line on the weight chart
    - npm run build exits 0 with no TypeScript errors
    - npm test exits 0 — all prior tests still pass
  </acceptance_criteria>
  <verify>
    <automated>npm run build 2>&1 && npm test 2>&1; echo "EXIT:$?"</automated>
    <fails_when>EXIT:1 in output, or "error TS" in build output, or any test "FAIL" in test output</fails_when>
  </verify>
  <done>Build and tests both pass. The weight chart screen shows a BMI section below the chart with current BMI value, category label with color, and a mini trend chart with threshold reference lines. No BMI section appears on any other metric's chart screen. When height is not set, "Set height in Settings to see BMI" shows instead of a crash or NaN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage → BMI calculation | Height value from 'myhealth-height' key is a user-entered string; must be parsed and validated before use in math |
| Dexie weights table → BMI data | Read-only query of all weight entries; local user data only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-08 | Tampering | useBmiData.ts — height divide-by-zero / NaN | medium | mitigate | Guard: if (!heightCm or heightCm <= 0 or isNaN(heightCm)) return error:'no-height' before any division; BmiSection renders safe message instead of crashing (RESEARCH.md Pitfall 5) |
| T-02-09 | Tampering | BmiSection CustomTooltip — XSS | low | accept | CustomTooltip uses React JSX; no dangerouslySetInnerHTML; BMI values are numbers from arithmetic, not user strings |
| T-02-10 | Denial of Service | useBmiData — full weights table scan | low | accept | db.weights.orderBy('date').toArray() scans all weight entries; for personal health data (likely < 10K entries) this is <50ms; acceptable tradeoff for full BMI history without date filtering |
</threat_model>

<verification>
After all tasks complete:
1. npm run build exits 0
2. npm test exits 0
3. grep -c "calcBmi" src/utils/bmi.ts returns at least 1
4. grep -c "no-height" src/hooks/useBmiData.ts returns at least 1 (height guard present)
5. grep -c "metric === 'weight'" src/components/Charts/MetricChart.tsx returns at least 1 (BMI conditional)
6. grep -c "BmiSection" src/components/Charts/MetricChart.tsx returns at least 2 (import + usage)
7. grep -c "ReferenceLine" src/components/Charts/BmiSection.tsx returns at least 1 (threshold lines)
8. grep -c "dangerouslySetInnerHTML" src/components/Charts/BmiSection.tsx returns 0 (no XSS vector)
</verification>

<success_criteria>
npm run build exits 0. npm test exits 0 with all BMI + aggregation tests passing. Weight chart screen shows BMI section below chart with current BMI, category, color hint, mini trend chart, and reference threshold lines. Setting height in Settings causes BMI to appear. Without height set, a safe message shows. No BMI section appears on any other metric's chart.
</success_criteria>

<output>
Create `.planning/phases/02-charts-visualization/02-04-SUMMARY.md` when done.

## Artifacts this phase produces

| Symbol | File | Consumed By |
|--------|------|-------------|
| calcBmi | src/utils/bmi.ts | useBmiData |
| bmiCategory | src/utils/bmi.ts | useBmiData, BmiSection |
| useBmiData | src/hooks/useBmiData.ts | BmiSection |
| BmiResult | src/hooks/useBmiData.ts | BmiSection |
| BmiSection | src/components/Charts/BmiSection.tsx | MetricChart.tsx (weight only) |
</output>
