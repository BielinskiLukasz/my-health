---
phase: 02-charts-visualization
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/hooks/useHeatmapData.ts
  - src/components/Charts/ActivityHeatmap.tsx
  - src/components/Dashboard/Dashboard.tsx
autonomous: true
requirements: [CHRT-04]

estimate:
  tokens: 65000
  raw_tokens: 32000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Dashboard shows a GitHub-style 52x7 CSS Grid heatmap below the metric tile grid (D-12)"
    - "Heatmap covers rolling 12 months (last 365 days), current day at right edge (D-11)"
    - "Cell color intensity encodes number of metrics logged that day: 0=grey, 1-2=light, 3-4=medium, 5-6=full (D-10)"
    - "Tapping a heatmap cell shows a tooltip with the date and which metrics were logged that day (D-13)"
    - "Heatmap is horizontally scrollable on small screens — does not overflow the viewport"
    - "npm run build exits 0 with no TypeScript errors"
  artifacts:
    - "src/hooks/useHeatmapData.ts — exports useHeatmapData() returning {cellData, isLoading}"
    - "src/components/Charts/ActivityHeatmap.tsx — 52x7 CSS Grid with intensity classes and tooltip"
    - "src/components/Dashboard/Dashboard.tsx — includes ActivityHeatmap below tile grid"
  key_links:
    - "useHeatmapData queries all 6 Dexie tables simultaneously — if any table query fails, heatmap shows empty"
    - "CSS Grid column-major layout (gridAutoFlow: column, gridTemplateRows: repeat(7, 10px)) is critical for correct week-column/day-row orientation"
    - "overflow-x-auto on container + minWidth: 600px on grid prevents viewport overflow on 375px phones (RESEARCH.md Pitfall 6)"
---

<objective>
Build the GitHub-style activity heatmap that appears on the Dashboard below the metric tile grid, showing 365 days of logging activity with per-cell intensity and a tap tooltip.

Purpose: Phase 2 success criterion 2 — users can view a year-view activity heatmap showing which days had any metric logged and how many metrics were logged that day.
Output: useHeatmapData hook, ActivityHeatmap component, Dashboard.tsx updated to render heatmap.
</objective>

<execution_context>
@C:/my-code/vibe-coding/my-health/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/my-health/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-charts-visualization/02-CONTEXT.md
@.planning/phases/02-charts-visualization/02-RESEARCH.md
@.planning/phases/02-charts-visualization/02-PATTERNS.md
@.planning/phases/02-charts-visualization/02-01-SUMMARY.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Heatmap data hook + CSS Grid component + Dashboard integration</name>
  <files>src/hooks/useHeatmapData.ts, src/components/Charts/ActivityHeatmap.tsx, src/components/Dashboard/Dashboard.tsx</files>
  <read_first>
    src/db/schema.ts
    src/components/Dashboard/Dashboard.tsx
    src/components/Dashboard/MetricTile.tsx
    .planning/phases/02-charts-visualization/02-RESEARCH.md
    .planning/phases/02-charts-visualization/02-PATTERNS.md
  </read_first>
  <action>
    STEP 1 — Create src/hooks/useHeatmapData.ts. This hook queries all 6 Dexie tables for the last 365 days and returns per-day logging counts.

    Export interface HeatmapCell as { date: string; count: number; metrics: string[] }.
    Export function useHeatmapData() returning { cellData: HeatmapCell[]; isLoading: boolean }.

    Implementation: Use the useEffect + async pattern with a cancelled flag (same pattern as MetricTile.tsx). Inside async load():
    1. Compute today = format(new Date(), 'yyyy-MM-dd') and start = format(subDays(new Date(), 364), 'yyyy-MM-dd') from date-fns.
    2. Query all 6 tables simultaneously using Promise.all: db.weights.where('date').between(start, today, true, true).toArray() for weights; same pattern for db.sleepEntries, db.stepEntries, db.waterEntries, db.heartRates, db.temperatures. Import db from "@/db/schema".
    3. Build a Map<string, string[]> from date to metric name array. For each weights entry add 'weight' to the date's array if not already present. For sleepEntries add 'sleep'. For stepEntries add 'steps'. For waterEntries add 'water'. For heartRates add 'heartRate'. For temperatures add 'temperature'. Use a Set to deduplicate (a metric can have multiple entries per day, but should count once in the heatmap).
    4. Generate all 365 dates from start to today using eachDayOfInterval from date-fns: eachDayOfInterval({ start: parseISO(start), end: parseISO(today) }). Format each date as 'yyyy-MM-dd'.
    5. Map each date to a HeatmapCell: { date, count: metricsMap.get(date)?.length ?? 0, metrics: metricsMap.get(date) ?? [] }.
    6. If not cancelled, call setCellData(cells).

    Security note for T-02-03: metric names in the 'metrics' array come from the hardcoded strings 'weight', 'sleep', 'steps', 'water', 'heartRate', 'temperature' — never from raw stored data — preventing heatmap tooltip injection.

    STEP 2 — Create src/components/Charts/ActivityHeatmap.tsx.

    Call useHeatmapData() for cell data.

    Helper function heatmapCellClass(count: number): string — returns 'bg-zinc-800' for 0, 'bg-emerald-900' for 1-2, 'bg-emerald-600' for 3-4, 'bg-emerald-500' for 5-6. This matches CONTEXT.md D-10 intensity levels.

    Render a titled section with heading "Activity" in text-sm font-medium text-zinc-400 mb-2.

    For month labels: compute the starting Monday of the first week to display. Extract unique year-month labels and their approximate column positions (count of weeks from start). Render month labels as a flex row above the grid using text-[10px] text-zinc-500 labels spaced to align with grid columns.

    Grid rendering: wrap the grid in div className="overflow-x-auto" (RESEARCH.md Pitfall 6). Inside that, render div with style={{ gridTemplateRows: 'repeat(7, 10px)', gridAutoFlow: 'column', display: 'grid', gap: '2px', minWidth: '600px' }}. Before rendering the 365 cells, pad the start of the grid with empty cells to align the first real day to its correct day-of-week row. Use getDay(parseISO(cells[0].date)) from date-fns to get the starting day (0=Sunday, 1=Monday, etc.). Render (startDay) extra div elements with className="w-[10px] h-[10px]" (transparent, no color) before the data cells. For each HeatmapCell, render div with className={"w-[10px] h-[10px] rounded-sm " + heatmapCellClass(cell.count)} and a data-date and data-metrics attribute for tooltip use. Apply onClick={() => handleCellClick(cell)} for tooltip (implemented in Task 2). For now, pass a no-op onClick.

    isLoading renders a div className="h-[100px] flex items-center justify-center text-zinc-500 text-sm" with text "Loading activity...".

    STEP 3 — Modify src/components/Dashboard/Dashboard.tsx. Import ActivityHeatmap from "@/components/Charts/ActivityHeatmap". After the closing tag of the 2-column metric tile grid div, add: div className="mt-6 px-4" containing ActivityHeatmap. This positions the heatmap below the tile grid per D-12.
  </action>
  <acceptance_criteria>
    - src/hooks/useHeatmapData.ts exists and exports useHeatmapData returning HeatmapCell[]
    - src/components/Charts/ActivityHeatmap.tsx uses CSS Grid with gridTemplateRows repeat(7, 10px) and gridAutoFlow column
    - ActivityHeatmap.tsx contains overflow-x-auto wrapper and minWidth: 600px on the grid
    - Dashboard.tsx imports and renders ActivityHeatmap after the metric tile grid
    - Metric names in HeatmapCell.metrics come from hardcoded strings, not raw stored data
    - npm run build exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run build 2>&1; echo "EXIT:$?"</automated>
    <fails_when>EXIT:1 in output, or "error TS" in build output</fails_when>
  </verify>
  <done>Build passes. Dashboard renders an activity heatmap below the metric tiles. The grid shows 365 days in a 52x7 column-major layout. Days with logged metrics show colored cells with emerald intensity. Days with no data show grey cells. The heatmap scrolls horizontally on small screens.</done>
</task>

<task type="auto">
  <name>Task 2: Add cell tap tooltip showing date and which metrics were logged (D-13)</name>
  <files>src/components/Charts/ActivityHeatmap.tsx</files>
  <read_first>
    src/components/Charts/ActivityHeatmap.tsx
    .planning/phases/02-charts-visualization/02-CONTEXT.md
    .planning/phases/02-charts-visualization/02-PATTERNS.md
  </read_first>
  <action>
    STEP 1 — Add tooltip state to ActivityHeatmap.tsx. Import useState from 'react'. Add state: const [tooltip, setTooltip] = useState<{ date: string; metrics: string[]; x: number; y: number } | null>(null).

    STEP 2 — Implement handleCellClick. The function receives a HeatmapCell and a React.MouseEvent. Extract clientX and clientY from the event. Call setTooltip({ date: cell.date, metrics: cell.metrics, x: event.clientX, y: event.clientY }).

    STEP 3 — Dismiss tooltip. Add onClick handler on the outer container div that calls setTooltip(null) when the click target is not a cell (or add a useEffect that listens for document click to dismiss). Simplest approach: add a fixed overlay div that appears when tooltip is non-null and calls setTooltip(null) on click, with z-index below the tooltip.

    STEP 4 — Render tooltip. When tooltip is non-null, render a fixed-position div with style={{ top: tooltip.y - 10, left: tooltip.x + 10 }} and className "fixed z-50 rounded-lg border border-zinc-700 bg-zinc-900 px-3 py-2 shadow-lg text-sm pointer-events-none". Inside render:
    - Date label: p className="text-zinc-400 text-xs mb-1" — format(parseISO(tooltip.date + 'T00:00:00'), 'EEEE, MMM d, yyyy') from date-fns.
    - If tooltip.metrics.length === 0: p className="text-zinc-500 text-xs" with text "No metrics logged".
    - Else: ul with li for each metric name, className "text-white text-xs" — render the human-readable label by looking up METRIC_CONFIG[m].label for each metric string m. Import METRIC_CONFIG from "@/utils/constants". Security: metric strings come from the hardcoded allowlist in useHeatmapData (T-02-03 mitigated — no raw stored data in tooltip).

    STEP 5 — Wire handleCellClick to each cell's onClick. Pass (cell, event) to handleCellClick. Cell divs get onClick={(e) => handleCellClick(cell, e)} and cursor-pointer className.

    STEP 6 — Verify build and review tooltip positioning. If tooltip renders off-screen on the right edge (when x > viewport width - 200), flip to left: left: tooltip.x - 160. Guard with a simple check: tooltip.x > window.innerWidth - 200 ? tooltip.x - 160 : tooltip.x + 10.
  </action>
  <acceptance_criteria>
    - ActivityHeatmap.tsx uses useState for tooltip state
    - Clicking a cell sets tooltip state with date and metrics array
    - Tooltip renders metric labels from METRIC_CONFIG, not raw stored metric key strings
    - Tooltip div has pointer-events-none and z-50
    - Tooltip dismiss mechanism exists (overlay or document click listener)
    - npm run build exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run build 2>&1; echo "EXIT:$?"</automated>
    <fails_when>EXIT:1 in output, or "error TS" in build output</fails_when>
  </verify>
  <done>Build passes. Tapping a heatmap cell shows a tooltip with the formatted date and a list of which metrics were logged that day. Cells with no logged metrics show "No metrics logged". The tooltip dismisses when tapping elsewhere. Metric names in the tooltip are human-readable labels (e.g., "Weight", "Sleep") from METRIC_CONFIG.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dexie IndexedDB → heatmap data | All 6 metric tables queried; data is own user data stored locally |
| Metric name strings → tooltip display | Metric names must come from hardcoded allowlist, not raw stored values |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-04 | Tampering | ActivityHeatmap tooltip — metric name injection | low | mitigate | Metric names in HeatmapCell.metrics come from hardcoded strings ('weight', 'sleep', etc.) set in useHeatmapData, not from raw Dexie field values; tooltip renders METRIC_CONFIG[m].label which is a static string — no injection vector |
| T-02-05 | Denial of Service | useHeatmapData — 365-day query across 6 tables | low | accept | All 6 queries run via Promise.all over local IndexedDB; no network requests; query time expected <100ms for typical personal data volumes; acceptable for dashboard load |
</threat_model>

<verification>
After all tasks complete:
1. npm run build exits 0
2. grep -rn "ActivityHeatmap" src/components/Dashboard/Dashboard.tsx returns at least 1 match
3. grep -c "overflow-x-auto" src/components/Charts/ActivityHeatmap.tsx returns at least 1
4. grep -c "gridAutoFlow" src/components/Charts/ActivityHeatmap.tsx returns at least 1
5. grep -c "useHeatmapData" src/components/Charts/ActivityHeatmap.tsx returns at least 1
6. grep -c "tooltip" src/components/Charts/ActivityHeatmap.tsx returns at least 1
</verification>

<success_criteria>
Dashboard shows a GitHub-style 52x7 activity heatmap below the metric tile grid. Cells are colored by logging intensity (grey to emerald). The heatmap scrolls horizontally on mobile. Tapping a cell shows date and logged metric names. Build passes with no TypeScript errors.
</success_criteria>

<output>
Create `.planning/phases/02-charts-visualization/02-02-SUMMARY.md` when done.

## Artifacts this phase produces

| Symbol | File | Consumed By |
|--------|------|-------------|
| useHeatmapData | src/hooks/useHeatmapData.ts | ActivityHeatmap |
| HeatmapCell | src/hooks/useHeatmapData.ts | ActivityHeatmap |
| ActivityHeatmap | src/components/Charts/ActivityHeatmap.tsx | Dashboard.tsx |
</output>
