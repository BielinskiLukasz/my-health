---
phase: 03-targets-goals
plan: 03
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - src/db/schema.ts
  - src/utils/targetCalcs.ts
  - src/utils/targetCalcs.test.ts
  - src/hooks/useTargetData.ts
  - src/hooks/useExerciseLogData.ts
  - src/components/Dashboard/ExerciseProxyTile.tsx
  - src/components/Dashboard/Dashboard.tsx
autonomous: true
requirements: [TARG-01, TARG-04, DASH-03]

estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The Dashboard always shows a 7th 'Exercise' tile (D-07), independent of whether its weekly target has been customized from the default."
    - "Tapping 'Mark as exercised' always writes to today's date (todayISO()), never to the Dashboard's currently-selected date, even if the user has navigated to a past date elsewhere in the app."
    - "The exercise proxy's streak counts consecutive WEEKS hitting the weekly target, never consecutive days (D-17) — its label always reads '{N} week streak', never '{N} day streak'."
    - "Editing the exercise weekly target does not reset the weekly streak (D-08's edit-persistence rule applied to the exercise proxy)."
  artifacts:
    - src/hooks/useExerciseLogData.ts
    - src/components/Dashboard/ExerciseProxyTile.tsx
  key_links:
    - "Dashboard.tsx -> ExerciseProxyTile -> useExerciseLogData -> db.exerciseLog"
    - "ExerciseProxyTile -> useTargetData('exercise') -> db.targets"
    - "ExerciseProxyTile -> targetCalcs.getExerciseWeeklyStatus / calculateWeeklyStreak"
  prohibitions:
    - requirement_id: TARG-01
      category: safety
      statement: "MUST NOT let the exercise-proxy 'mark as exercised' toggle write to any date other than today's canonical todayISO() key — a date-key mistake would silently corrupt a different day's own logged history, and per D-01 this table's shape must carry forward cleanly into Phase 4's real session data."
---

<objective>
Deliver TARG-01's "exercise frequency" target as the lightweight placeholder D-01 calls for: a
7th Dashboard tile backed by a new minimal `exerciseLog` table (one boolean row per day), a
settable weekly session-count target (reusing Plan 03-01's `targets` table with a pseudo-key),
a same-day toggle, a weekly on-track color, and a consecutive-WEEK streak (reusing Plan 03-02's
`calculateWeeklyStreak`). This is the last expansion off the Plan 03-01 tracer, closing out the
full requirement set for Phase 3.

Purpose: TARG-01 explicitly lists "exercise frequency" among the metrics that need a settable
target; D-01 requires this now as a stopgap, not deferred to Phase 4.
Output: Dexie schema v5 (`exerciseLog` table), a small `getExerciseWeeklyStatus` addition to
`targetCalcs.ts`, `useExerciseLogData` hook, `ExerciseProxyTile` component, and its slot on
`Dashboard.tsx`.
</objective>

<execution_context>
@C:/my-code/vibe-coding/my-health/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/my-health/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-targets-goals/03-CONTEXT.md
@.planning/phases/03-targets-goals/03-RESEARCH.md
@.planning/phases/03-targets-goals/03-UI-SPEC.md

@src/db/schema.ts
@src/utils/targetCalcs.ts
@src/hooks/useTargetData.ts
@src/components/Dashboard/MetricTile.tsx
@src/components/Dashboard/Dashboard.tsx
@src/hooks/useChartData.ts
@src/utils/dateFormat.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Exercise weekly-status function + Dexie v5 + widen useTargetData for the exercise pseudo-metric</name>
  <files>src/db/schema.ts, src/utils/targetCalcs.ts, src/utils/targetCalcs.test.ts, src/hooks/useTargetData.ts</files>
  <read_first>
    src/db/schema.ts (version(1) through version(4) additive-chaining history from Plans 03-01/
    03-02 — this task adds version(5), never touching earlier versions);
    src/utils/targetCalcs.ts, src/hooks/useTargetData.ts (both from Plan 03-01);
    .planning/phases/03-targets-goals/03-CONTEXT.md D-01, D-07, D-17;
    .planning/phases/03-targets-goals/03-UI-SPEC.md "Exercise Proxy Tile" section (weekly status
    thresholds: green = hit target, yellow = below by 1-2, red = below by more than 2).
  </read_first>
  <reversibility rating="costly">
    D-01: the exerciseLog table's one-row-per-day boolean shape should carry forward or migrate
    cleanly into Phase 4's real training-session schema; a mismatched shape means writing a
    migration script later. Kept intentionally minimal (date + boolean only) to make that future
    migration as low-risk as possible.
  </reversibility>
  <behavior>
    - getExerciseWeeklyStatus(weekCount, weeklyTarget) returns "green" when weekCount >=
      weeklyTarget.
    - returns "yellow" when weeklyTarget - weekCount is 1 or 2.
    - returns "red" when weeklyTarget - weekCount is greater than 2.
    - there is no "grey" case — once a weekly target exists, a week always has a defined count
      (D-05 already hides the whole exercise status area when no target is set).
  </behavior>
  <action>
    Add `this.version(5).stores({ exerciseLog: "date" })` to `MyHealthDB`'s constructor in
    `src/db/schema.ts`, chained AFTER `version(4)` (additive only, per D-19; versions 1-4 stay
    untouched). Add interface `ExerciseLog { date: string; logged: boolean }` and class property
    `exerciseLog!: EntityTable<ExerciseLog, "date">`.

    Widen the existing `Target` interface's `metric` field (already `MetricType | "exercise"`
    from Plan 03-01) — no further schema change needed there. Widen `useTargetData`'s exported
    parameter type if it was narrowed anywhere to just `MetricType`, so `useTargetData("exercise")`
    reads/writes `db.targets.get("exercise")` / `.put({ metric: "exercise", value, createdAt })`
    exactly like any other metric, always omitting `targetDate` (the exercise proxy is an
    ongoing weekly cadence goal, not a deadline — Claude's discretion, since D-06's `/chart/
    :metric` convention has no analogous screen for the exercise proxy; the tile itself is the
    closest available surface, see Task 2).

    RED first: extend `targetCalcs.test.ts` with `describe("getExerciseWeeklyStatus", ...)`
    covering every case in `<behavior>`. Run `npm test -- targetCalcs` and confirm it fails
    (function not exported yet).

    GREEN: add `getExerciseWeeklyStatus(weekCount: number, weeklyTarget: number): "green" |
    "yellow" | "red"` to `targetCalcs.ts` — `weekCount >= weeklyTarget -> "green"`; `weeklyTarget
    - weekCount <= 2 -> "yellow"`; else `"red"`. Re-run `npm test -- targetCalcs` and confirm it
    passes.
  </action>
  <verify>
    <automated>npm test -- targetCalcs 2>&1; echo "EXIT:$?"</automated>
    <fails_when>exit code is non-zero, OR any line contains "FAIL" or "✗".</fails_when>
  </verify>
  <verify>
    <automated>npm run build 2>&1; echo "EXIT:$?"</automated>
    <fails_when>exit code is non-zero, OR output contains "error TS".</fails_when>
  </verify>
  <acceptance_criteria>
    - `getExerciseWeeklyStatus` has no "grey" branch in its implementation.
    - Dexie is at schema version 5 with `targets` (v3), `personalBests` (v4), and `exerciseLog`
      (v5) all present and queryable; versions 1-2 (weights/sleep/steps/water/heartRate,
      temperatures) are unmodified.
  </acceptance_criteria>
  <done>The exercise proxy has its own settable target (reusing the `targets` table) and its
  own weekly status function, both unit-tested.</done>
</task>

<task type="auto">
  <name>Task 2: Exercise proxy hook + 7th Dashboard tile</name>
  <files>src/hooks/useExerciseLogData.ts, src/components/Dashboard/ExerciseProxyTile.tsx, src/components/Dashboard/Dashboard.tsx</files>
  <read_first>
    src/components/Dashboard/MetricTile.tsx (tile card shell/style, lines 181-230);
    src/components/Dashboard/Dashboard.tsx (grid layout, lines 7-35 — full-width `col-span-2`
    pattern already used for heartRate/temperature);
    src/hooks/useChartData.ts (useEffect+async+cancelled hook pattern);
    src/utils/targetCalcs.ts (calculateWeeklyStreak from Plan 03-02, getExerciseWeeklyStatus
    from Task 1);
    src/hooks/useTargetData.ts;
    .planning/phases/03-targets-goals/03-UI-SPEC.md "Exercise Proxy Tile (7th Dashboard Tile)"
    and "Dashboard Layout (Updated for Phase 3)" sections.
  </read_first>
  <action>
    Create `src/hooks/useExerciseLogData.ts`: compute the current ISO week (Monday-Sunday) via
    date-fns `startOfWeek(new Date(), { weekStartsOn: 1 })` / `endOfWeek(new Date(), {
    weekStartsOn: 1 })`, formatted `yyyy-MM-dd`. Query `db.exerciseLog.where("date")
    .between(weekStart, weekEnd, true, true).toArray()`; count entries where `logged === true`
    as `weekCount`. Compute `todayLogged` via `(await db.exerciseLog.get(todayISO()))?.logged ??
    false`. Expose `toggleToday(): Promise<void>` that ALWAYS writes using `todayISO()` as the
    primary key — `db.exerciseLog.put({ date: todayISO(), logged: !todayLogged })` — regardless
    of the Dashboard's Zustand `currentDate` (this is the exact mitigation for the prohibition
    above: never let a selected-but-not-today date leak into this write). Also compute the last
    52 weeks' `{ met: boolean }[]` history (each week's count vs. the current weekly target,
    most-recent-first) for `calculateWeeklyStreak`. Return `{ weekCount, todayLogged,
    toggleToday, weeklyMetHistory, isLoading }`. Follow the useEffect+async+cancelled pattern
    throughout.

    Create `src/components/Dashboard/ExerciseProxyTile.tsx`: same card shell as `MetricTile`
    (`rounded-2xl bg-zinc-950 p-4 border-l-[3px] w-full`), but using the fixed hex `#EC4899`
    (UI-SPEC, not in `METRIC_CONFIG`/Tailwind's default palette) applied via inline `style=
    {{ borderColor: "#EC4899" }}` / `style={{ color: "#EC4899" }}` rather than a Tailwind class.
    Content: label "EXERCISE" (uppercase, 12px, pink); count display "{weekCount}/{weeklyTarget}
    this week" (20px semibold); a full-width toggle button "Mark as exercised" that becomes
    "Exercised today ✓" (different style) once `todayLogged` is true, calling `toggleToday()` on
    tap; a status badge via `getExerciseWeeklyStatus(weekCount, weeklyTarget)` (same
    green/yellow/red pill styling as other tiles, no grey case); a streak label "{N} week
    streak" via `calculateWeeklyStreak(weeklyMetHistory)`. `weeklyTarget` comes from
    `useTargetData("exercise")`; if no target row exists yet, default `weeklyTarget = 3`
    (Claude's discretion — a sensible starting cadence) and render the target number as tappable
    inline-editable: tapping it swaps the static text for a `<input type="number" min="1"
    max="14">`; on blur/Enter with a valid integer, call `saveTarget({ value: newValue })` (no
    `targetDate`) and revert to static display.

    Wire into `Dashboard.tsx`: insert `<div className="col-span-2"><ExerciseProxyTile /></div>`
    immediately after the existing temperature tile block and before the `<div
    className="mt-6"><ActivityHeatmap /></div>` line — matching the existing heartRate/
    temperature full-width single-tile pattern (D-07's "7th tile").
  </action>
  <verify>
    <automated>npm run build 2>&1; echo "EXIT:$?"</automated>
    <fails_when>exit code is non-zero, OR output contains "error TS".</fails_when>
  </verify>
  <verify>
    <human-check>On the Dashboard, the 7th "Exercise" tile shows "0/3 this week" with a "Mark as
    exercised" button; tapping it flips to "Exercised today ✓" and the count becomes "1/3"; the
    status badge and week-streak label update accordingly. Tap the target number, change it to
    5, and confirm the tile now reads "1/5 this week" without the streak resetting.</human-check>
  </verify>
  <acceptance_criteria>
    - `toggleToday` always keys off `todayISO()`, never the Dashboard's `currentDate` from
      Zustand, even when `currentDate` has been navigated to a past date.
    - `ExerciseProxyTile` renders unconditionally as the Dashboard's 7th tile, using the default
      `weeklyTarget = 3` before any target row exists.
    - The streak label text always reads "{N} week streak", never "{N} day streak".
  </acceptance_criteria>
  <done>The exercise-frequency proxy is a fully working 7th Dashboard tile — settable weekly
  target, same-day toggle, weekly status color, and consecutive-week streak — end to end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User toggle tap -> exerciseLog write | The only user-triggered write in this plan; must key off a fixed, non-selectable date. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-07 | Tampering | exerciseLog toggle write path | high | mitigate | `toggleToday` always writes using `todayISO()` as the primary key, never the Dashboard's selectable `currentDate`, preventing a wrong-day write that would corrupt the user's own history (satisfies the prohibition above). |
| T-03-08 | Denial of Service | useExerciseLogData weekly count query | low | mitigate | The query is bounded to the current ISO week (Monday-Sunday, at most 7 rows) via date-fns `startOfWeek`/`endOfWeek`, never a full-table scan. |
| T-03-09 | Tampering | Inline weekly-target editor (no chart screen exists for the exercise proxy) | low | mitigate | Same numeric-range validation as `TargetModal` (positive integer, 1-14 sessions/week) is applied before the Dexie write. |
</threat_model>

<verification>
- `npm test -- targetCalcs` passes (including the new `getExerciseWeeklyStatus` cases).
- `npm run build` passes after both tasks.
- Manual: toggling "Mark as exercised" today never alters any other date's `exerciseLog` row
  even after navigating the Dashboard's date elsewhere and back.
</verification>

<success_criteria>
- ROADMAP Phase 3 success criterion 1 is completed for the "exercise frequency" metric named in
  TARG-01, which Plan 03-01 intentionally deferred (it only covers weight/sleep/steps/water/
  heartRate).
- The Dashboard's 7th tile (D-07) exists and behaves per D-01/D-17.
</success_criteria>

## Artifacts this phase produces (this plan's contribution)

- `src/hooks/useExerciseLogData.ts`
- `src/components/Dashboard/ExerciseProxyTile.tsx`
- `src/db/schema.ts` — Dexie version 5, `exerciseLog` table + `ExerciseLog` interface.
- `src/utils/targetCalcs.ts` extended with `getExerciseWeeklyStatus`.

<output>
Create `.planning/phases/03-targets-goals/03-03-SUMMARY.md` when done
</output>
