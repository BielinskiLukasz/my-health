---
phase: quick
plan: 260917-g44
type: execute
wave: 1
depends_on: []
files_modified:
  - src/utils/chartDomain.ts
  - src/utils/chartDomain.test.ts
  - src/components/Charts/MetricChart.tsx
  - .planning/phases/02-charts-visualization/02-UAT.md
autonomous: true
requirements: [G-02-5]

estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Weight, heart rate, and temperature charts scale their Y-axis to the min/max of the data shown for the selected period (W/M/Y), instead of always starting at 0."
    - "Steps, sleep, and water charts are visually unchanged — their Y-axis still starts at 0, since those metrics are counts/durations."
    - "02-UAT.md records G-02-5 as resolved with root_cause and a real fix-commit reference, matching the pattern used for G-02-1/G-02-2."
  artifacts:
    - src/utils/chartDomain.ts
    - src/utils/chartDomain.test.ts
    - src/components/Charts/MetricChart.tsx
    - .planning/phases/02-charts-visualization/02-UAT.md
  key_links:
    - "MetricChart.tsx's <YAxis domain={...}> on both the LineChart branch and the BarChart branch receives getYAxisDomain(metric) — the same per-metric domain must apply regardless of chart type, since the yearly view (period='Y') forces a bar chart even for weight/heartRate/temperature (D-06)."
---

<objective>
Fix gap G-02-5: on the chart screen (`/chart/:metric`), weight, heart rate, and temperature charts render their Recharts `<YAxis>` with no `domain` prop set, so the axis always starts at 0 — flattening real trends for metrics whose values never approach 0 (weight hovering around ~70kg, heart rate around ~60-80bpm, temperature around ~36.5°C). Add a small, testable `getYAxisDomain(metric)` utility that returns a `['dataMin - N', 'dataMax + N']` Recharts domain-formula tuple for exactly these three metrics — steps, sleep, and water are counts/durations and legitimately keep their existing 0-start axis, unchanged. Wire the utility into both chart-type branches in `MetricChart.tsx`, then mark G-02-5 resolved in `02-UAT.md`.

Purpose: Unblock UAT test 1 (D2 gap) so Phase 02 verification can proceed past this "major" severity issue.
Output: New `src/utils/chartDomain.ts` utility + `chartDomain.test.ts`; updated `MetricChart.tsx` YAxis wiring; updated `02-UAT.md` gap entry for G-02-5 marked resolved.
</objective>

<execution_context>
@C:/my-code/vibe-coding/my-health/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/my-health/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@C:/my-code/vibe-coding/my-health/.planning/STATE.md
@C:/my-code/vibe-coding/my-health/.planning/phases/02-charts-visualization/02-UAT.md
@C:/my-code/vibe-coding/my-health/src/components/Charts/MetricChart.tsx
@C:/my-code/vibe-coding/my-health/src/hooks/useChartData.ts
@C:/my-code/vibe-coding/my-health/src/utils/constants.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scale Y-axis to data min/max for weight, heart rate, temperature charts</name>
  <files>src/utils/chartDomain.ts, src/utils/chartDomain.test.ts, src/components/Charts/MetricChart.tsx</files>
  <behavior>
    - getYAxisDomain("weight") returns ["dataMin - 1", "dataMax + 1"]
    - getYAxisDomain("heartRate") returns ["dataMin - 5", "dataMax + 5"]
    - getYAxisDomain("temperature") returns ["dataMin - 0.3", "dataMax + 0.3"]
    - getYAxisDomain("steps"), getYAxisDomain("sleep"), getYAxisDomain("water") all return undefined (unchanged 0-start behavior)
  </behavior>
  <action>
  Create `src/utils/chartDomain.ts` exporting a single function `getYAxisDomain(metric: MetricType): [string, string] | undefined` (import `MetricType` from `@/store/appStore`, matching the import convention already used in `ChartHeader.tsx`). Internally define a `MIN_MAX_SCALE_METRICS` list containing exactly `"weight"`, `"heartRate"`, `"temperature"` (per G-02-5 — steps/sleep/water are counts/durations that legitimately start at 0 and must NOT be touched), and a `Y_AXIS_PADDING` map keyed by those three metrics with fixed padding values matching each metric's unit from `METRIC_CONFIG` (`src/utils/constants.ts`): weight 1 (kg), heartRate 5 (bpm), temperature 0.3 (°C). When `metric` is not in `MIN_MAX_SCALE_METRICS`, return `undefined`; otherwise return the tuple `["dataMin - " + padding, "dataMax + " + padding]`. Recharts' `YAxis` `domain` prop natively parses this `'dataMin - N'` / `'dataMax + N'` string-formula syntax (confirmed in `node_modules/recharts/types/cartesian/YAxis.d.ts`, recharts ^3.10 currently installed) and computes the actual min/max from whichever dataset is currently rendered, so the padding is always relative to the filtered period (W/M/Y) already produced by `useChartData`.

  Write the RED test first in `src/utils/chartDomain.test.ts` (vitest, `environment: "node"` per `vite.config.ts` — no DOM needed for a pure function) asserting the four behaviors listed above, confirm it fails against a stub/missing implementation, then implement `chartDomain.ts` until it passes.

  Wire the result into `MetricChart.tsx`: import `getYAxisDomain` from `@/utils/chartDomain`, compute `const yAxisDomain = getYAxisDomain(metric)` once near the top of the component (alongside the existing `config`/`accentHex` declarations), then add `domain={yAxisDomain}` to BOTH `<YAxis>` elements — the one inside the `LineChart` branch (currently around lines 125-129) and the one inside the `BarChart` branch (currently around lines 153-157). Do not add a `domain` prop anywhere else, do not change `axisLine`/`tickLine`/`tick` on either `YAxis`, and do not touch `ChartHeader.tsx`, `BmiSection.tsx`, or `useChartData.ts` — this is a display-only Y-axis change; the underlying data/stat calculations are already correct. Because `yAxisDomain` is `undefined` for steps/sleep/water (and their yearly bar view), passing `domain={undefined}` to `YAxis` is a no-op and preserves today's default (0-start) behavior for those metrics exactly.
  </action>
  <verify>
    <automated>npm test -- src/utils/chartDomain.test.ts && grep -c "domain={yAxisDomain}" src/components/Charts/MetricChart.tsx</automated>
  </verify>
  <done>`chartDomain.test.ts` passes (4 assertions); `MetricChart.tsx` passes `yAxisDomain` (from `getYAxisDomain(metric)`) to both the line-chart and bar-chart `YAxis` elements (grep count 2); `npm run build` exits 0 with no TypeScript errors; opening `/chart/weight`, `/chart/heartRate`, or `/chart/temperature` (any period) renders a Y-axis scaled to that period's data range instead of starting at 0, while `/chart/steps`, `/chart/sleep`, and `/chart/water` remain visually unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Mark G-02-5 resolved in 02-UAT.md</name>
  <files>.planning/phases/02-charts-visualization/02-UAT.md</files>
  <action>
  In `02-UAT.md`'s `## Gaps` section, update ONLY the `gap_id: G-02-5` entry (truth: "Charts for weight, heart rate, and temperature scale their Y-axis to the min/max of the selected period, not from 0.") to mirror how the G-02-2 entry below it records resolution: change `status: open` to `status: resolved`, and add a `root_cause` field after `test: 1` (before `artifacts`), plus `resolved_by` and `resolved_at` fields after `missing` — matching the G-02-2 field ordering. Set `root_cause` to: every metric chart's Recharts `YAxis` had no `domain` prop set, so Recharts computed a default range that always included 0 regardless of the actual data range, flattening trend visibility for weight/heart-rate/temperature whose real values never approach 0. Set `resolved_by` to reference Task 1's commit using the same phrasing style as G-02-2 (`"commit {SHORT_SHA} (fix(charts): ...)"`) — fill in the real short SHA via `git log -1 --format=%h` immediately after Task 1's commit is made. Set `resolved_at: 2026-09-17`. Leave `artifacts: []` and `missing: []` as-is (no path objects to add — this fix is a display-only Y-axis change, not a structural bug). Do NOT touch the G-02-3 or G-02-4 entries (owned by a parallel in-flight quick task), do not touch G-02-1/G-02-2, the `## Current Test`, `## Tests`, or `## Summary` sections, and do not renumber or reorder gap entries.
  </action>
  <verify>
    <automated>grep -A6 "gap_id: G-02-5" .planning/phases/02-charts-visualization/02-UAT.md | grep -c "status: resolved"</automated>
  </verify>
  <done>The G-02-5 entry in 02-UAT.md has `status: resolved`, `root_cause`, `resolved_by` (with a real commit SHA, not a placeholder), and `resolved_at: 2026-09-17`; the G-02-3 and G-02-4 entries are untouched (still `status: open`).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | Pure client-side chart rendering change (Y-axis domain); no new input surface, no new dependency, no network call |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-QUICK-01 | N/A | N/A | low | accept | No security-relevant surface touched — a pure display/axis-scaling change plus a markdown status update. |
</threat_model>

<verification>
- `npm test -- src/utils/chartDomain.test.ts` passes (4 assertions)
- `grep -c "domain={yAxisDomain}" src/components/Charts/MetricChart.tsx` returns 2
- `npm run build` exits 0 (TypeScript + Vite build clean)
- `grep -A6 "gap_id: G-02-5" .planning/phases/02-charts-visualization/02-UAT.md | grep -c "status: resolved"` returns 1
- Manual/visual: open `/chart/weight`, `/chart/heartRate`, `/chart/temperature` (each period W/M/Y) — Y-axis is scaled to that data's range, not starting at 0; open `/chart/steps`, `/chart/sleep`, `/chart/water` — unchanged, Y-axis still starts at 0
</verification>

<success_criteria>
- Weight, heart rate, and temperature charts scale their Y-axis to the selected period's data min/max on all three periods (W/M/Y), including the yearly bar-chart view
- Steps, sleep, and water charts are visually unchanged (Y-axis still starts at 0)
- 02-UAT.md's G-02-5 gap entry is marked `resolved` with a real fix-commit reference, following the G-02-1/G-02-2 pattern; G-02-3/G-02-4 entries remain untouched
</success_criteria>

<output>
Create `.planning/quick/260917-g44-fix-gap-g-02-5-weight-heart-rate-and-tem/260917-g44-SUMMARY.md` when done
</output>
