---
phase: quick
plan: 260918-ep1
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Dashboard/MetricTile.tsx
  - .planning/phases/02-charts-visualization/02-UAT.md
autonomous: true
requirements: [QUICK-260918-EP1]

estimate:
  tokens: 13000
  raw_tokens: 13000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "MetricTile's variant B (not logged today) shows both the last-logged-date text and a \"Not logged today\" badge again, restoring the information removed in commit 8d38c9f."
    - "The date and badge render as one compact single-line row (smaller text/padding than the pre-8d38c9f two-line layout) so they do not force the taller, Y-axis-scaled 14-day Sparkline (height=60, min/max domain for weight/heartRate/temperature) back down."
    - "Variant A (logged today) and variant C (no data ever) tile rendering is unchanged."
    - "02-UAT.md's gap G-02-6 entry records resolved_by/resolved_at/fix_committed for the fix commit while status stays 'failed' (unresolved) pending human re-verification."
    - "02-UAT.md's test 6 (DASH-02) entry keeps result: issue but its note documents that the fix has landed and is awaiting re-verification."
  artifacts:
    - src/components/Dashboard/MetricTile.tsx
    - .planning/phases/02-charts-visualization/02-UAT.md
  key_links:
    - "MetricTile.tsx's restored `{lastDate && ...}` date span and unconditional \"Not logged today\" badge sit inside the same variant-b `<div className=\"flex flex-col gap-1\">` wrapper as the existing value+unit row and the `<Sparkline .../>` call rendered below it — order and wrapper must stay intact so the Sparkline keeps rendering directly under the tile's text content, unchanged at height={60}."
    - "02-UAT.md's G-02-6 gap entry and its Tests section test-6 entry both reference the same fix commit hash from Task 1, keeping the paper trail traceable from gap to code to pending re-verification."
---

<objective>
Fix UAT gap G-02-6: commit 8d38c9f (quick task 260917-h77) removed the last-logged-date line and "Not logged today" badge from `MetricTile`'s variant B to free up vertical space for a taller, Y-axis-scaled 14-day sparkline. The user flagged this removal as a regression during UAT ("the not logged message and date of log are not visible in home page now"). Restore both pieces of information WITHOUT reverting the sparkline improvement — combine the date and badge into one compact single-line row (smaller text/padding than the original two-line layout) that fits alongside the taller sparkline instead of competing with it.

Purpose: Close gap G-02-6 by giving the user back information they rely on (when a metric was last logged, and that today has no entry) without regressing the Y-axis min/max scaling fix that solved a real readability problem in the same commit.
Output: `MetricTile.tsx` variant B restores the last-logged-date + badge as one compact row; `02-UAT.md` records the fix commit against gap G-02-6 and test 6, without flipping either to resolved/passed (that still needs human re-verification).
</objective>

<execution_context>
@C:/my-code/vibe-coding/my-health/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/my-health/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@C:/my-code/vibe-coding/my-health/.planning/STATE.md
@C:/my-code/vibe-coding/my-health/.planning/phases/02-charts-visualization/02-UAT.md
@C:/my-code/vibe-coding/my-health/src/utils/dateFormat.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restore compact last-logged-date + "Not logged today" badge in MetricTile variant B (G-02-6)</name>
  <files>src/components/Dashboard/MetricTile.tsx</files>
  <action>
  Re-add the import removed in commit 8d38c9f: `import { formatShortDate } from "@/utils/dateFormat"`, placed back alongside the other imports at the top of the file (it was originally between the `useAppStore` import and the `date-fns` import). Restore `lastDate` in the tile-data destructuring: change `const { variant, value, error } = tileData` back to `const { variant, value, lastDate, error } = tileData`.

  In the variant "b" JSX branch (the `<div className="flex flex-col gap-1">` block that currently contains only the value+unit row), add ONE new sibling `<div>` immediately after the existing `<div className="flex items-baseline gap-1">...</div>` value+unit row, and before the closing tag of the `flex flex-col gap-1` wrapper. This new row combines the last-logged-date text and the "Not logged today" badge on a single flex line — deliberately more compact than the pre-8d38c9f original (which stacked a `text-sm` date line and a separately-margined `text-xs` badge as two stacked lines), so the row adds minimal height and the `<Sparkline metric={metric} accentHex={CHART_HEX[metric]} height={60} />` call below it (added by commit 8d38c9f, must stay exactly as-is) still has room to render at its full 60px height. The new row: a `<div className="flex items-center gap-2">` wrapping (a) `{lastDate && (<span className="text-xs text-gray-400">{formatShortDate(lastDate)}</span>)}` and (b) an unconditional `<span className="rounded-full bg-zinc-800 px-1.5 py-0.5 text-[10px] uppercase tracking-wide text-gray-400">Not logged today</span>`. This mirrors the pre-8d38c9f original's conditional split exactly — the badge always renders in variant b, the date only renders when `lastDate` is set (defensively; every variant-b return path in `loadTileData` already sets `lastDate`) — but drops the original's larger text sizes (`text-sm`/`text-xs`) and separate-line `mt-1` badge margin down to `text-xs`/`text-[10px]` and `px-1.5 py-0.5` on one row, so the two pieces of information cost roughly one compact line of height instead of two.

  Do not touch the `Sparkline` call, the `getYAxisDomain`/Y-axis min/max scaling wiring inside `Sparkline.tsx` or `chartDomain.ts`, or the variant "a" (logged today) / variant "c" (no data ever) JSX branches — this task is scoped to variant "b" only.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint && npm test && grep -q "formatShortDate" src/components/Dashboard/MetricTile.tsx && grep -q "Not logged today" src/components/Dashboard/MetricTile.tsx && grep -q "height={60}" src/components/Dashboard/MetricTile.tsx</automated>
  </verify>
  <done>MetricTile.tsx re-imports `formatShortDate`, restores `lastDate` in the destructuring, and variant B renders a compact single-line row (date + "Not logged today" badge) below the value+unit row and above the unchanged `height={60}` Sparkline. Variant A and C branches are untouched. `npm run typecheck`, `npm run lint`, and `npm test` all pass with no errors.</done>
</task>

<task type="auto">
  <name>Task 2: Record the fix in 02-UAT.md against gap G-02-6 and test 6, without flipping status</name>
  <files>.planning/phases/02-charts-visualization/02-UAT.md</files>
  <action>
  Capture the commit hash Task 1 produced (e.g. via `git log -1 --format=%H -- src/components/Dashboard/MetricTile.tsx`, or reuse the hash already reported from Task 1's own commit step) — call it `<hash>` below.

  In the `## Gaps` section, find the `gap_id: G-02-6` entry and add three new fields immediately after its existing `missing: []` line (matching the field order/style already used on the resolved gap entries above it, e.g. G-02-5): `resolved_by: "commit <hash> (fix: restore last-logged-date and not-logged badge to dashboard tile)"`, `resolved_at: 2026-09-18`, and `fix_committed: true`. Do NOT change the existing `status: failed` line on this entry — it must stay `failed` (not `resolved`) because a visual layout change still needs a human UAT pass to confirm it looks right.

  In the `## Tests` section, find test `6. DASH-02 — Dashboard sparklines + tile layout`. Leave `result: issue` and its `reported`/`severity` fields exactly as they are. Append one sentence to the end of its existing `note:` field (do not replace the existing note text — concatenate) stating that the fix landed in commit `<hash>` (restoring the last-logged-date line and "Not logged today" badge as one compact row alongside the taller Y-axis-scaled sparkline from commit 8d38c9f) and is awaiting human re-verification.
  </action>
  <verify>
    <automated>grep -A 12 "gap_id: G-02-6" .planning/phases/02-charts-visualization/02-UAT.md | grep -q "fix_committed: true" && grep -A 12 "gap_id: G-02-6" .planning/phases/02-charts-visualization/02-UAT.md | grep -q "status: failed" && grep -A 12 "gap_id: G-02-6" .planning/phases/02-charts-visualization/02-UAT.md | grep -q "resolved_by:" && grep -A 8 "^### 6. DASH-02" .planning/phases/02-charts-visualization/02-UAT.md | grep -q "result: issue"</automated>
  </verify>
  <done>02-UAT.md's `gap_id: G-02-6` entry has new `resolved_by`, `resolved_at: 2026-09-18`, and `fix_committed: true` fields while `status: failed` is unchanged. Test 6 (`DASH-02`)'s `result: issue` and other fields are unchanged, and its `note:` now also references the fix commit hash and states it awaits re-verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | Pure client-side layout restoration in an existing UI component, plus a docs-only YAML/markdown update; no new input surface, no new dependency, no network call |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-QUICK-260918-EP1-01 | N/A | N/A | low | accept | No security-relevant surface touched — a display-only restoration of already-shipped, non-sensitive fields (a local date already stored in IndexedDB, and a static status label) plus a UAT tracking-doc edit. |
</threat_model>

<verification>
- `npm run typecheck` exits 0
- `npm run lint` exits 0
- `npm test` passes (existing suites unaffected)
- `grep -q "formatShortDate" src/components/Dashboard/MetricTile.tsx` and `grep -q "Not logged today" src/components/Dashboard/MetricTile.tsx` both match — date and badge are back
- `grep -q "height={60}" src/components/Dashboard/MetricTile.tsx` — the taller, Y-axis-scaled sparkline from 8d38c9f is untouched
- 02-UAT.md's `gap_id: G-02-6` entry carries `resolved_by`/`resolved_at`/`fix_committed: true` but still reads `status: failed`
- 02-UAT.md's test 6 (`DASH-02`) entry still reads `result: issue`, with its `note:` extended to reference the fix commit and awaiting re-verification
- Manual/visual: open the Dashboard — any tile in the "not logged today" state (variant b) shows its value, then one compact line with the last-logged date and a "Not logged today" badge, then the 60px-tall sparkline (scaled to min/max for weight/heart-rate/temperature); tiles with today's data (variant a) and tiles with no data at all (variant c) look exactly as before
</verification>

<success_criteria>
- MetricTile variant B shows the last-logged-date text and "Not logged today" badge again, as one compact row, closing gap G-02-6's reported regression
- The 14-day sparkline keeps rendering at height=60 with the Y-axis min/max scaling from commit 8d38c9f — that improvement is not reverted
- Variant A and variant C rendering is unchanged
- 02-UAT.md's G-02-6 gap and test 6 entries record the fix commit (resolved_by/resolved_at/fix_committed) without flipping status/result — both remain open for human re-verification
</success_criteria>

<output>
Create `.planning/quick/260918-ep1-fix-uat-gap-g-02-6-restore-the-last-logg/260918-ep1-SUMMARY.md` when done
</output>